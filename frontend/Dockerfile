FROM node:24-alpine

WORKDIR /app
COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 80
# Run the Vite dev server bound to port 80
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "80"]
