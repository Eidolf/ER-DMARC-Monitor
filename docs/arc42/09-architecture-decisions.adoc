= 09. Architecture Decisions

== Architecture Decision Records (ADRs)
* ADR 001: Use docToolchain / Asciidoctor for Docs-as-Code pipeline (`scripts/build-architecture-docs.sh`).
* ADR 002: Manual trigger strategy (`workflow_dispatch`) for GitHub Actions documentation pipeline to eliminate token waste (`.github/workflows/architecture-docs-manual.yml`).

// TODO: REVIEW REQUIRED - Record future architectural decision records here when major design choices are made.
